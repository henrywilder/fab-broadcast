# -------------------------------------------------------------------
# FAB Broadcast Tool — Environment Variables
# -------------------------------------------------------------------
# Copy this file to .env.local and fill in your values.
# These are needed for the overlay state sync (via Upstash Redis).
#
# How to get these values:
#   1. Go to https://upstash.com and create a free account
#   2. Create a new Redis database (free tier is fine)
#   3. On the database page, click "REST API"
#   4. Copy the UPSTASH_REDIS_REST_URL and UPSTASH_REDIS_REST_TOKEN values
# -------------------------------------------------------------------

UPSTASH_REDIS_REST_URL=https://your-database.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-token-here
